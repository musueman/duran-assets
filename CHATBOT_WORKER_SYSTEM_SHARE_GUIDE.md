# Chatbot-Worker Turn System 공유용 설명서

## 이 시스템은 무엇인가

이 시스템은 챗봇이 긴 서사를 직접 화면에 흘려보내는 대신, 정해진 슬롯 값을 URL에 담아 Worker를 호출하고, Worker가 그 값을 받아 하나의 시각적 턴 화면으로 렌더링하는 구조다.

핵심 역할 분리는 단순하다.

- 챗봇: 장면, 대사, 상태 변화, 다음 선택지를 슬롯 값으로 작성한다.
- Worker: 슬롯 값을 받아 카드, 판정, 상태창, 대사창, 장면 UI를 일관된 화면으로 출력한다.
- 상태 주석: 다음 턴을 이어가기 위한 최소 상태를 HTML 주석 형태로 남긴다.
- 디버그 명령: 출력이 깨졌을 때 사용자가 `!debug` 한 줄로 직전 응답을 검사하고 재출력하게 한다.

즉, AI를 룰 엔진이나 UI 엔진으로 쓰지 않고, "서사 슬롯 작성기"로 제한한다. 화면 구성과 판정 표시, 반복되는 UI 규칙은 Worker가 담당한다.

## 왜 이런 구조를 쓰는가

일반적인 챗봇 진행은 턴이 길어질수록 형식이 흔들린다. 상태창을 빼먹거나, 규칙 설명을 본문에 섞거나, 다음 장면까지 과하게 진행하는 문제가 생긴다.

이 시스템은 출력 형식을 고정한다.

```text
상단 고정 이미지 또는 헤더
Worker 호출 이미지 URL
<!--STATE ... -->
```

챗봇은 이 3줄 밖으로 본문을 쓰면 실패 출력으로 간주한다. 모든 서사와 대사는 Worker URL 내부의 본문 슬롯으로 들어간다. 따라서 표시 형식은 매번 같고, 내용만 바뀐다.

## 활용 가능한 방식

이 구조는 특정 장르에 묶이지 않는다. 아래처럼 사용할 수 있다.

- TRPG/솔로 RPG: 판정값, 장면, 보상, 피해, 다음 선택지를 한 화면으로 표시한다.
- 비주얼 노벨: 챗봇이 대사와 상황을 만들고 Worker가 대사창과 캐릭터 컷인을 렌더링한다.
- 교육 시뮬레이션: 학습자가 선택을 입력하면 Worker가 결과, 점수, 피드백을 카드 형태로 보여준다.
- 상담형 워크플로우: 답변 요약, 다음 질문, 위험 신호, 진행 상태를 구조화된 패널로 보여준다.
- 게임 마스터 도구: AI가 장면을 생성하되 상태와 턴 진행은 고정 스키마로 관리한다.

## 강점

첫째, 형식 안정성이 높다.
챗봇의 자유 출력 영역을 줄이고, 결과를 Worker 화면에 넣기 때문에 출력 형식이 덜 무너진다.

둘째, 책임 분리가 명확하다.
AI는 장면을 쓰고, Worker는 UI와 반복 규칙을 처리한다. 프롬프트가 모든 시각 요소를 설명할 필요가 없다.

셋째, 수정과 테스트가 쉽다.
룰 설명을 바꾸는 일과 화면 렌더링을 고치는 일이 분리된다. Worker는 dev/prod로 나누어 실험 후 본 시스템에 반영할 수 있다.

넷째, 복구 절차가 단순하다.
출력이 Worker 밖으로 새거나 URL이 깨지면 사용자는 `!debug`만 입력한다. 챗봇은 새 턴을 만들지 않고 직전 응답을 같은 상태로 재출력한다.

다섯째, 다른 프로젝트에 이식하기 쉽다.
세계관, 카드명, 상태명, 이미지 URL만 바꾸면 같은 골격을 다른 장르에 적용할 수 있다.

## 지금까지 거친 주요 수정과 이유

1. 챗봇 출력 구조를 3줄로 고정했다.
   이유: 챗봇이 Worker 밖에서 서사를 이어 쓰면 화면형 시스템의 의미가 사라진다. 본문은 URL 슬롯 안에만 있어야 한다.

2. 본문 슬롯을 명확히 나누었다.
   이유: "지난 선택의 결과"와 "다음 선택 직전 상황"을 분리해야 턴 진행이 과도하게 앞서가지 않는다.

3. URL 예약문자 치환 규칙을 추가했다.
   이유: 챗봇이 만든 Markdown 이미지 URL 안에 `&`, `#`, `%`, `?`, `=` 같은 문자가 그대로 들어가면 query가 깨진다.

4. `!debug` 명령을 추가했다.
   이유: 긴 OOC 지시문은 모델이 일반 입력으로 오해할 수 있다. 짧은 고정 호출문이 분기 조건으로 더 안정적이다.

5. dev/prod Worker를 분리했다.
   이유: 실험 기능이 본 챗봇에 바로 들어가면 진행 중인 세션이 깨질 수 있다. dev에서 확인한 뒤 prod에 반영한다.

6. 용어를 통일했다.
   이유: 같은 개념을 `body`, `본문`, `trace`, `결과문`처럼 여러 이름으로 부르면 챗봇과 Worker가 서로 다른 값을 기대하게 된다.

7. 시스템, 로어북, 캐릭터 정보를 분리했다.
   이유: 핵심 출력 규칙은 짧게 유지하고, 세계관/인물/세부 규칙은 별도 자료로 빼야 유지보수가 쉽다.

8. Worker의 HTTP 처리와 fallback을 정리했다.
   이유: `HEAD`, `OPTIONS`, CORS, 누락 asset fallback이 불안하면 챗봇은 정상 URL을 만들었는데도 화면이 깨질 수 있다.

## 이 시스템을 어떻게 쓸 수 있는가

기본 도입 순서는 다음과 같다.

1. 한 턴에 필요한 슬롯을 정한다.
   예: 상태, 이전 결과문, 다음 상황문, 판정값, 보상, 피해, 장면 분위기.

2. Worker URL 스키마를 만든다.
   예: `/turn.svg?topState=...&bottomState=...&resultBody=...&nextBody=...`

3. 챗봇 시스템에 출력 계약을 넣는다.
   예: "응답은 헤더 이미지, Worker URL, STATE 주석 3줄만 출력한다."

4. 상태 주석을 만든다.
   예: `<!--STATE PHASE=LOOP SCENE=forest TURN=4 TARGET=6 -->`

5. 디버그 호출을 만든다.
   예: `!debug`는 새 턴이 아니라 직전 출력 검사 및 재출력이다.

6. dev/prod Worker를 나눈다.
   예: 테스트 챗봇은 `worker-dev.example.com`, 본 챗봇은 `worker.example.com`을 쓴다.

## 사용한 Worker 구성: 분리에서 통합까지

이 프로젝트는 처음부터 하나의 큰 Worker로 시작하지 않았다. 기능을 작게 나누어 검증한 뒤, 실제 챗봇 진행에 필요한 화면은 통합 Turn Worker로 모았다.

### 1단계: 기능별 분리 Worker

초기에는 각 Worker가 하나의 문제만 맡았다.

| Worker | 주 역할 | 대표 엔드포인트 | 활용 방식 |
| --- | --- | --- | --- |
| Dice Worker | 주사위, 카드, 카드+주사위 합성 | `/dice.webp`, `/dice.svg`, `/card.svg`, `/manifest.json` | 챗봇이 만든 판정값을 시각적 주사위와 카드 이미지로 표시 |
| Scene/Sprite Worker | 환경/카드/감정 기반 장면 SVG | `/scene.svg`, `/duran.svg` | 현재 환경, 카드 유형, 관계, 분위기, 감정을 받아 장면 애니메이션 출력 |
| Dialogue Worker | 대사 패널과 초상/표정 선택 | `/chat.svg`, `/dialogue`, `/line.txt`, `/markdown`, `/portrait.webp` | 챗봇이 작성한 대사를 생성하지 않고 그대로 받아 대사창으로 렌더링 |
| Action Worker | 행동 컷인, 성공/실패 연출 | `/action.svg`, `/cutin.svg`, `/health.json` | 판정 결과와 행동 맥락에 맞춰 배경, 표정, 효과를 합성 |
| Turn Worker | 한 턴 전체 UI 통합 | `/turn.svg`, `/trace.svg`, `/veil.svg`, `/health.json` | 결과, 대사, 장면, 새 상황, 상태를 하나의 턴 패널로 출력 |

분리 Worker의 장점은 테스트가 쉽다는 점이다. 예를 들어 대사창이 깨졌으면 Dialogue Worker만 보면 되고, 주사위 합성이 깨졌으면 Dice Worker만 보면 된다. 프롬프트도 각 기능별로 짧게 실험할 수 있다.

### 2단계: 챗봇이 여러 Worker를 직접 호출하던 방식

초기 구조에서는 챗봇 출력 안에 여러 이미지 URL이 들어갈 수 있었다.

```text
상태창
주사위/카드 Worker URL
장면 Worker URL
대사 Worker URL
다음 상태 주석
```

이 방식은 구현이 빠르지만 문제가 있었다.

- 챗봇이 URL을 여러 개 만들수록 실수 지점이 늘어난다.
- 본문이 Worker 밖으로 새기 쉽다.
- 모바일에서 카드, 장면, 대사 패널의 크기와 순서가 불안정해진다.
- dev/prod 주소가 섞이면 테스트용 Worker가 본 챗봇에 연결될 수 있다.
- 각 Worker의 결과가 서로 다른 턴처럼 보일 수 있다.

따라서 여러 Worker를 "챗봇이 직접 조립"하는 구조에서, 하나의 Turn Worker가 "내부에서 조립"하는 구조로 이동했다.

### 3단계: 호환 연결과 라우팅 정리

통합 전환 중에는 기존 호출을 바로 버리지 않고 호환 경로를 두었다.

- Scene/Sprite Worker의 `/dchat.svg`는 Dialogue Worker의 `/chat.svg`로 넘기는 호환 경로로 사용했다.
- dev Worker는 dev Dialogue로, prod Worker는 prod Dialogue로만 연결되게 했다.
- `OPTIONS`, `HEAD`, CORS 처리를 정리해 브라우저와 미리보기 환경에서 안정적으로 동작하게 했다.
- 누락된 이미지 asset은 fallback을 두어 화면 전체가 깨지지 않게 했다.

이 단계의 목적은 "기존 챗봇 출력이 어느 정도 계속 작동하면서도 새 구조로 옮겨갈 수 있게 하는 것"이었다.

### 4단계: Turn Worker로 통합

최종 진행용 화면은 Turn Worker가 맡는다.

Turn Worker는 `/turn.svg` 하나로 다음 요소를 함께 렌더링한다.

- 지난 입력의 결과 영역
- 판정값, 목표값, 보정값
- 피해/보상/변화 표시
- 대사 또는 컷씬 대사
- 다음 상황문
- 새 카드/새 목표값
- 상단/하단 상태 표시
- 모바일/데스크톱 레이아웃 차이

중요한 점은 Turn Worker가 모든 Worker를 완전히 폐기한 것이 아니라는 점이다. Turn Worker는 필요한 기능을 통합 오케스트레이터처럼 다룬다.

- Dice Worker의 카드/주사위 렌더링은 Turn Worker 내부 화면에서 재사용된다.
- Scene/Sprite Worker의 장면 SVG는 Turn Worker가 필요한 위치에 삽입한다.
- Dialogue/Action Worker에서 검증한 표정, 대사, 컷인 아이디어는 Turn Worker 안의 통합 화면 로직으로 흡수됐다.
- `/trace.svg`, `/veil.svg`는 이전 결과 영역과 새 상황 영역을 따로 확인하기 위한 호환/테스트 엔드포인트로 남겼다.

즉, 통합의 핵심은 "모든 기능을 한 파일에 욱여넣기"가 아니라 "챗봇이 호출해야 하는 표면을 하나로 줄이기"다.

### 5단계: 챗봇 시스템 단순화

Turn Worker 통합 후 챗봇 시스템은 이렇게 단순해졌다.

```text
상단 고정 이미지
turn.svg Worker URL
GM_STATE 주석
```

챗봇은 더 이상 카드 이미지, 대사 이미지, 장면 이미지를 따로 조립하지 않는다. 대신 `turn.svg`에 필요한 슬롯만 채운다.

이 방식의 결과는 명확하다.

- 챗봇 프롬프트가 짧아진다.
- 출력 형식이 덜 흔들린다.
- 디버그 기준이 단순해진다.
- UI 수정은 Worker에서 처리하고, 서사 수정은 프롬프트/로어북에서 처리할 수 있다.

## 다른 사람이 활용할 때의 Worker 설계 기준

처음부터 통합 Worker 하나로 만들 필요는 없다. 기능 검증 단계에서는 분리하는 편이 낫다.

권장 순서는 다음과 같다.

1. Dice/Score Worker
   숫자, 점수, 판정, 카드처럼 규칙적인 시각 요소를 먼저 분리한다.

2. Scene Worker
   장소, 분위기, 감정, 장면 유형을 받아 배경이나 장면 패널을 렌더링한다.

3. Dialogue Worker
   챗봇이 작성한 대사를 받아 대사창, 초상, 말풍선으로 표시한다.

4. Action/Result Worker
   성공, 실패, 위험, 보상 같은 결과 연출을 처리한다.

5. Turn Worker
   위 요소들을 한 화면으로 묶어 실제 챗봇이 호출할 최종 엔드포인트를 만든다.

프로젝트 규모가 작다면 처음부터 Turn Worker만 만들어도 된다. 다만 어떤 기능이 문제인지 빠르게 분리해 확인하려면, 최소한 내부 함수 수준에서는 Dice/Scene/Dialogue/Action 역할을 나누는 편이 좋다.

## 일반화한 통합 구조 예시

아래는 특정 프로젝트 이름을 제거한 통합형 구조다.

```text
score-worker.example.com
  /dice.svg
  /card.svg

scene-worker.example.com
  /scene.svg

dialogue-worker.example.com
  /chat.svg
  /portrait.webp

result-worker.example.com
  /action.svg

turn-worker.example.com
  /turn.svg   <- 챗봇이 최종적으로 호출하는 주소
  /trace.svg  <- 이전 결과 영역 테스트
  /veil.svg   <- 다음 상황 영역 테스트
```

운영 단계의 챗봇은 가능하면 `turn-worker.example.com/turn.svg`만 호출한다. 나머지 Worker는 Turn Worker 내부에서 부르거나, 테스트/호환/개별 미리보기 용도로 둔다.

## 뜯어 쓰기용 시스템 전문

아래는 특정 세계관을 제거한 주석형 전문이다.
`# 주석:` 줄은 설명용이다. 다른 사람이 실제 챗봇 시스템에 넣을 때는 그대로 둬도 되지만, 토큰을 아끼려면 프로젝트에 맞게 수정한 뒤 주석 줄은 지우는 편이 좋다.

```text
[Chatbot-Worker Turn System]

# 주석: 이 시스템의 목적은 챗봇을 "서사 슬롯 작성기"로 제한하고,
# Worker 또는 외부 렌더러가 화면 UI와 반복 표시를 맡게 하는 것이다.

[용어]
# 주석: 프로젝트에 맞게 이름을 바꿔도 된다. 단, 한 번 정한 이름은 시스템/로어북/Worker에서 동일하게 쓴다.
PL=사용자 또는 플레이어.
PC=장면 속 주인공 또는 대상 인물.
GM=진행자 역할의 챗봇.
상단상태=화면 위에 표시할 짧은 상태 요약.
하단상태=화면 아래에 표시할 상세 상태.
결과본문=지난 사용자 입력이 만든 결과문.
다음본문=다음 사용자 입력이 필요한 현재 상황문.
컷씬대사=화면 말풍선 또는 대사창에 넣을 한 줄 대사.
목표값=이번 판정 또는 장면의 난도.
직전목표값=지난 턴의 목표값.

[출력]
응답은 아래 3줄만 출력한다.
![]({상단고정이미지URL})
renderURL
<!--STATE PHASE=LOOP SCENE={장면키} TURN={턴수} TARGET={목표값} FLAGS={상태플래그} -->

# 주석: 상단고정이미지URL은 없으면 제거하고 renderURL+STATE 2줄 구조로 바꿔도 된다.
# 주석: STATE는 다음 턴을 이어가기 위한 최소 정보다. 사람이 읽을 수 있는 짧은 키=값 형태를 권장한다.

완료 기준: 응답 마지막 글자는 STATE를 닫는 `>` 이다.
출력검사: 3줄 외 텍스트는 실패. 서사/대사/상태는 renderURL 값 안으로 옮기거나 삭제한다. STATE 뒤 출력 금지.

renderURL=`![]({WORKER_URL}/turn.svg?topState={상단상태}&bottomState={하단상태}&roll={{d6,1}}&action={행동요약}&stat={분류}&ability={능력값}&item={소비값}&bonus={보정값}&target={직전목표값}&damage={피해}&reward={보상}&change={변화}&resultBody=BODY_START~p~{결과본문}~p~BODY_END&nextTarget={목표값}&sceneType={장면유형}&mood={분위기}&emotion={감정}&nextBody=BODY_START~p~{다음본문}~p~BODY_END&line={컷씬대사})`

# 주석: WORKER_URL에는 본인이 배포한 Worker 주소를 넣는다.
# 예: https://worker.example.com
# 주석: roll={{d6,1}} 같은 매크로는 사용하는 챗봇 플랫폼의 주사위/랜덤 문법에 맞게 바꾼다.
# 주석: 판정이 없는 프로젝트라면 roll/stat/ability/item/bonus/target을 제거해도 된다.

[값]
공백=+, 문단=~p~, 줄바꿈=~n~.
URL 예약문자 치환=&→~amp~, #→~hash~, %→~pct~, ?→~q~, =→~eq~.
본문/상태/대사 값에는 원문 예약문자를 남기지 않는다.

# 주석: 챗봇이 Markdown 이미지 URL을 직접 만들기 때문에 URL 깨짐 방지가 중요하다.
# 주석: Worker 쪽에서 다시 decode할 치환 규칙과 시스템의 치환 규칙이 같아야 한다.

[DEBUG]
PL입력 `!debug`=직전 출력검사.
3줄 외 출력/STATE뒤서술/URL값깨짐은 같은 값/STATE로 재출력한다.
정상이면 마지막 정상 3줄만 재출력한다.
새턴/새판정/목표값/턴수/상태갱신 금지.

# 주석: !debug는 플레이어 행동이 아니라 복구 명령이다.
# 주석: 챗봇이 사과하거나 설명하지 않고, 바로 3줄 출력만 다시 하게 만드는 것이 목적이다.

[턴]
첫 턴: 상단고정이미지→renderURL→STATE.
루프: PL 입력을 직전 다음본문의 해결 행동으로 처리한다.
결과본문=지난 입력의 결과문. 행동, 반응, 판정 결과, 피해/보상/변화를 반영해 장면을 닫는다.
다음본문=다음 입력이 필요한 상황문. 새 단서, 새 위험, 다음 선택지를 제시한다.
다음본문은 PL 입력 직전에서 멈추고 후속 해결을 쓰지 않는다.

# 주석: 핵심은 결과본문과 다음본문을 분리하는 것이다.
# 결과본문은 "방금 한 행동의 결과", 다음본문은 "이제 무엇을 할지 물어보는 상황"이다.

[본문]
지문과 대사는 모두 결과본문/다음본문 값으로 변환해 보낸다.
화면 본문 슬롯은 URL 내부뿐이고, STATE 뒤에는 서술 슬롯이 없다.
본문이 길면 압축하고 renderURL 밖에 이어 쓰지 않는다.
대사 형식=`이름 | 대사`.
대사는 앞뒤를 ~p~로 분리한다.
컷씬대사에는 화면에 강조할 한 줄만 넣는다.

# 주석: Worker가 말풍선, 대사창, 카드 본문 등을 나누어 렌더링할 수 있게
# 본문과 강조 대사를 별도 슬롯으로 나누는 방식이다.

[상태]
프로젝트에서 추적할 자원을 정한다.
예: HP/점수/진행도/신뢰도/소지품/위험도/학습진도.
실제 증감·획득·상실만 피해/보상/변화와 하단상태에 반영한다.
상태가 바뀌지 않았으면 기존 값을 유지한다.

# 주석: 상태를 매번 새로 창작하게 두면 장기 진행에서 깨진다.
# 이전 STATE와 이전 화면의 상태를 기준으로 필요한 변화만 반영하게 한다.

[STATE]
STATE는 응답 마지막 줄이다.
형식=<!--STATE PHASE=LOOP SCENE={장면키} TURN={턴수} TARGET={목표값} FLAGS={상태플래그} -->.
PHASE=LOOP/END.
SCENE=현재 장면 또는 위치 키.
TURN=현재 턴 번호.
TARGET=이번 목표값 또는 NONE.
FLAGS=필요한 상태 플래그. 없으면 NONE.

# 주석: STATE는 다음 응답의 기준점이다.
# 너무 많은 정보를 넣지 말고, 다음 턴 계산에 필요한 최소값만 넣는다.
```

## 우리 시스템 전용 요소와 일반화 예시

아래 항목은 원본 시스템에 묶인 고유 요소다. 다른 프로젝트에서는 오른쪽처럼 바꾸면 된다.

| 원본 시스템 전용 요소 | 일반화 예시 |
| --- | --- |
| 판타지 카드 RPG | 조사 게임, 상담 플로우, 학습 퀘스트, 추리 시뮬레이션 |
| `운명의흔적본문` | `resultBody`, `previousResult`, `resolutionText` |
| `새운명의패본문` | `nextBody`, `nextPrompt`, `currentChallenge` |
| `FATE`, `LASTFATE` | `target`, `previousTarget`, `difficulty` |
| `상단ST`, `하단ST` | `topState`, `bottomState`, `summaryState`, `detailState` |
| 카드: 위협/교환/조우/조사/이동/정비 | 장면유형: 위험/거래/만남/탐색/이동/회복 |
| 환경: 전장/숲/폐허/성벽 등 | 장소: 사무실/거리/학교/우주선/던전/상담실 |
| 특정 캐릭터와 세계관 | 프로젝트별 주인공, 안내자, NPC, 사용자 역할 |
| Cloudflare Worker URL | 본인이 배포한 Worker, 서버리스 함수, 이미지 렌더 API |

## 적용 예시: 추리 게임 버전

```text
renderURL=`![](https://mystery-worker.example.com/turn.svg?caseState={caseState}&suspectState={suspectState}&roll={{d6,1}}&action={investigation}&target={difficulty}&resultBody=BODY_START~p~{조사결과}~p~BODY_END&nextBody=BODY_START~p~{다음단서와선택지}~p~BODY_END&line={탐정대사})`
```

이 예시에서 챗봇은 탐정 소설을 길게 직접 출력하지 않는다. 조사 결과와 다음 단서를 URL 슬롯에 넣고, Worker가 사건판, 용의자 상태, 단서 카드, 대사창을 렌더링한다.

## 적용 예시: 학습 퀘스트 버전

```text
renderURL=`![](https://study-worker.example.com/lesson.svg?progress={progress}&score={score}&topic={topic}&feedback=BODY_START~p~{피드백}~p~BODY_END&nextTask=BODY_START~p~{다음문제}~p~BODY_END)`
```

이 예시에서 챗봇은 학생의 답을 평가해 피드백과 다음 문제를 슬롯으로 보낸다. Worker는 점수, 진행도, 해설, 다음 과제를 일정한 화면으로 표시한다.

## 설계할 때 주의할 점

- URL 슬롯 이름은 한 번 정하면 바꾸지 않는다.
- 챗봇이 직접 출력하는 줄 수를 최소화한다.
- 긴 서사는 URL 내부 본문 슬롯에 넣되, 너무 길면 압축하게 한다.
- 예약문자 치환 규칙은 시스템에 반드시 넣는다.
- 상태 주석은 사람이 읽을 수 있게 짧고 안정적으로 둔다.
- `!debug` 같은 단문 복구 명령을 넣는다.
- 실험용 Worker와 실제 서비스 Worker를 분리한다.

## 결론

이 시스템은 "챗봇 기반 턴 진행을 Worker 렌더링과 결합하는 방식"이다.
챗봇은 창의적인 텍스트를 만들고, Worker는 반복되는 화면과 규칙 표시를 안정적으로 처리한다.

따라서 이 구조는 장르형 챗봇, 게임 진행, 교육 시뮬레이션, 상담형 워크플로우처럼 "AI가 내용을 만들지만 출력 형식은 고정되어야 하는 프로젝트"에 적합하다.

핵심만 가져가려면 세 가지를 가져가면 된다.

1. 출력은 고정된 3줄로 제한한다.
2. 모든 본문은 Worker URL 슬롯 안에 넣는다.
3. `!debug`로 직전 출력만 검사하고 새 턴을 만들지 않는다.
