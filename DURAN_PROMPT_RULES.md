# Duran Prompt Rules

이 문서는 시스템, 로어북, 세계관, 캐릭터 정보를 수정할 때 계속 참고하는 기준이다.

## 목표 기조

- AI는 룰 엔진이 아니라 서술 엔진이다.
- 주사위 계산, 판정 표기, 결과 표기는 SVG/워커가 맡는다.
- 프롬프트는 AI가 채울 슬롯을 작고 선명하게 만든다.
- 모델의 자유 영역은 지문, 분위기, 감정, 장면 묘사다.
- 시스템은 핵심 출력 뼈대만 남기고, 참고 정보는 로어북/세계관/캐릭터 정보로 분리한다.
- 시스템과 세계관은 분량 제한을 두지 않는다.
- 로어북은 각 항목 500자 제한을 지킨다.
- 캐릭터 정보는 요약 100자, 상세 800자 제한을 지킨다.

## 설계 원칙

- 금지 목록보다 출력할 슬롯과 순서를 먼저 준다.
- 같은 뜻의 규칙은 한 곳에만 둔다.
- 용어는 반드시 정본 명칭 하나로 통일하고, 시스템·로어북·워커 호출 예시에서 같은 이름을 쓴다.
- 계산 설명보다 URL 슬롯 구조를 우선한다.
- 워커 화면형 출력은 경계가 명확해야 한다. 챗봇의 자연어 서술은 모두 URL 슬롯 안으로 넣고, Worker 밖에는 고정 출력 줄 외 텍스트를 두지 않는다.
- 가벼운 모델이 훑어도 남는 짧은 문장과 고정 구조를 쓴다.
- “하지 않음”을 길게 쓰기보다 해당 행동을 유도하는 단어를 본문 구조에서 빼는 쪽을 우선한다.

## 현재 구조

- `00_현재_진행시스템.txt`: KEY, URL, FLOW, TURN, TEXT, STATE, GS만 관리한다.
- `01_ST.txt`: 상단/하단 ST 형식만 관리한다.
- `02_CARD_색틈.txt`: 카드 성격, 관계/분위기 기본값, 색틈 단서만 관리한다.
- `04_새운명의패SVG.txt`: 새운명의패본문과 패/환경/관계/분위기/감정/FATE 슬롯만 관리한다.
- `05_판정_피해보상.txt`: 분류, 소비품/색틈 숫자, 피해/보상/변화 후보만 관리한다.
- `세계관_진행축.txt`: 배경 진행축과 세계 흐름을 관리한다.
- `character_info`: 인물 성격, 비밀, 등장 조건의 정본이다.

## 용어 체계

- `PL`: 플레이어, `{{user}}`.
- `DU`: 듀란.
- `VM`: 장막 목소리, `{{char}}`.
- `P`: turnURL의 d6 파라미터명. 플레이어 의미로 쓰지 않는다.
- `FATE`: 새운명의패 목표값. AI가 1~8 정수 하나를 직접 고른다.
- `LASTFATE`: 직전 새운명의패 값.
- `상단ST`: turnURL의 상단 공개 상태값.
- `하단ST`: turnURL의 하단 공개 상태값.
- `운명의흔적본문`: 지난 패 해결문.
- `새운명의패본문`: 다음 패 개시문.
- `컷씬대사`: 운명의흔적 컷씬에 들어갈 `이름 | 내용` 값.

시스템·로어북·워커 호출 예시는 위 정본 용어만 기록한다.
`상단ST` 값은 `🗓날짜 | 📍현장~n~[카드] [환경] [진행]` 두 줄 형식으로 통일한다.
환경 진행은 환경별 누적 횟수 기준이다. GS `HD`의 `전장n|숲n|폐허n|성벽n|검문소n|지하n|실내n` 형식을 정본으로 쓴다.
URL 값은 공백 `+`, 문단 `~p~`, 줄바꿈 `~n~`을 쓰고, 예약문자는 `&→~amp~`, `#→~hash~`, `%→~pct~`, `?→~q~`, `=→~eq~`로 치환한다.

## 판정 기준

- 실제 주사위 매크로는 turnURL의 `{{d6,1}}` 하나만 쓴다.
- AI는 `ability`, `item`, `tint`, `damage`, `reward`, `change`, `FATE` 슬롯을 채운다.
- 이전 SVG 텍스트/URL이 판정 정본이다.
- 본문과 ST가 SVG와 충돌하면 SVG를 기준으로 다음 턴을 잇는다.
- 응답이 `상단 고정 이미지`, `turnURL`, `GS` 3줄 밖으로 서사를 이어가면 실패 출력이다. 추가 서사는 URL 본문 슬롯으로 옮기거나 삭제한다.
- `새운명의패본문`은 다음 사용자 입력이 필요한 선택 지점에서 멈춘다. 다음 패 해결이나 후속 장면을 GS 뒤에 쓰지 않는다.
- `!debug` 입력은 판정 대상이 아니다. `act/stat/ability/item/tint/FATE/ENV/ENVN/HD/DONE`을 새로 만들지 않고 직전 실패 응답을 같은 상태로 검사/재출력한다.

## 수정 방법

- 먼저 중복 규칙을 찾고, 가장 적합한 한 파일에만 남긴다.
- 부정 지시는 긍정 슬롯 지시로 바꾼다.
- 로어북은 500자 안에서 핵심 슬롯과 참조 정보만 남긴다.
- 캐릭터 요약 정보는 100자, 캐릭터 상세 정보는 800자 안에서 관리한다.
- 새 캐릭터는 캐릭터 정보에 넣고, 시스템 문구를 늘리지 않는다.
- 새 워커/이미지는 시스템에 직접 넣지 않고 전용 로어북으로 연결한다.
- 원본과 테스트 폴더는 같은 기준으로 동기화한다.

## 사용자 디버그 구문

워커 밖 서술, GS 뒤 출력, URL 값 깨짐이 보이면 사용자는 한 줄만 입력한다.
`!debug`

`!debug`는 새 턴이 아니라 직전 응답 검사다. 챗봇은 같은 값과 같은 GM_STATE를 유지하고, 실패 텍스트를 URL 본문 슬롯으로 옮기거나 삭제한 뒤 3줄만 다시 출력해야 한다.

## 워커 기준

- 워커는 전달받은 입력 순서와 값을 정확히 보기 좋게 출력한다.
- 워커 호출 파라미터도 정본 용어를 우선한다. 상태는 `상단ST/하단ST`, 본문은 `운명의흔적본문/새운명의패본문`으로 통일한다.
- 챗봇이 직접 만드는 이미지 URL은 깨지면 워커가 복구할 수 없으므로 본문/상태/컷씬 값에는 URL 예약문자를 원문 그대로 두지 않는다.
- 워커 밖 출력은 워커가 통제할 수 없다. 시스템 프롬프트가 `GS 뒤 출력 금지`, `3줄 외 텍스트 실패`, `길면 압축`을 직접 가져야 한다.
- AI가 흔들려도 워커가 받은 값을 정규화해 표기한다.
- 랜덤 반응은 기존 애니메이션과 랜덤 기능을 보존한다.
- 새 기능은 dev 워커와 서브 챗봇에서 먼저 검증한 뒤 prod로 올린다.
